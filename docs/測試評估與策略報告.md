[{'type': 'text', 'text': '現在我已經收集到足夠的資訊。讓我生成完整的測試評估報告。\n\n# FlowCraft 測試評估與策略報告\n\n## 1. 執行摘要\n\n**專案概況**：\n- **專案類型**：工作流自動化引擎（類似 Zapier/n8n）\n- **技術棧**：Vue 3 (前端) + Node.js/Express (後端) + TypeScript\n- **程式碼規模**：約 5,108 行 TypeScript (後端)\n- **核心模組**：14 個 Executors，4 個 Stores，1 個 WorkflowEngine\n\n**測試現狀總結**：\n- ❌ **零測試涵蓋率** - 專案中沒有任何正式的測試檔案\n- ❌ 沒有測試框架配置（無 vitest.config.ts / jest.config.js）\n- ❌ 沒有 CI/CD 測試流程（無 GitHub Actions）\n- ❌ 沒有測試文件或測試指南\n- ⚠️ 發現 1 個臨時測試檔案：`server/test-method4.js`（非正式測試）\n\n**風險等級**：🔴 **Critical** - 核心工作流引擎和 14 個 Executors 完全沒有測試覆蓋\n\n**Quick Wins 機會**：\n1. 為 WorkflowEngine 添加單元測試（3-5 天可完成基礎覆蓋）\n2. 為 3 個高頻 Executors 添加單元測試（gemini, send-telegram, youtube-monitor）\n3. 添加 API endpoint 整合測試（1-2 天）\n\n---\n\n## 2. 測試現狀分析\n\n### 2.1 測試檔案掃描結果\n\n**實際測試檔案**：0 個\n- ❌ 沒有 `*.test.ts` 或 `*.spec.ts` 檔案\n- ❌ 沒有 `__tests__` 目錄\n\n**測試框架配置**：無\n- ❌ 沒有 `vitest.config.ts`\n- ❌ 沒有 `jest.config.js`\n- ✅ 專案使用 Vite，建議使用 Vitest（原生整合）\n\n**發現的臨時測試檔案**：\n```\nserver/test-method4.js  # 臨時測試腳本，非正式測試\n```\n\n### 2.2 測試框架與工具\n\n**package.json 依賴分析**：\n\n**前端（根目錄）**：\n```json\n{\n  "devDependencies": {\n    "vite": "^7.3.1",           // ✅ 可直接整合 Vitest\n    "typescript": "~5.9.3"       // ✅ TypeScript 支援\n  }\n}\n```\n\n**後端（server/）**：\n```json\n{\n  "devDependencies": {\n    "typescript": "^5.3.3",\n    "ts-node-dev": "^2.0.0"     // ✅ 開發時 TS 執行\n  }\n}\n```\n\n**建議新增的測試依賴**：\n```json\n{\n  "devDependencies": {\n    "vitest": "^1.2.0",           // 單元測試框架\n    "@vue/test-utils": "^2.4.4",  // Vue 組件測試\n    "playwright": "^1.41.0",      // E2E 測試（專案已用 Patchright）\n    "msw": "^2.1.0",              // API Mocking\n    "c8": "^9.1.0"                // 測試覆蓋率\n  }\n}\n```\n\n### 2.3 測試類型分佈\n\n| 測試類型 | 當前狀態 | 應有數量 | 優先級 |\n|---------|---------|---------|--------|\n| **Unit Tests** | 0 | 50+ | 🔴 Critical |\n| **Integration Tests** | 0 | 15+ | 🟡 High |\n| **E2E Tests** | 0 | 5+ | 🟢 Nice-to-have |\n| **Component Tests** | 0 | 10+ | 🟡 High |\n\n---\n\n## 3. 核心模組測試需求分析\n\n### 3.1 WorkflowEngine（優先級：🔴 Critical）\n\n**檔案位置**：`server/src/execution/WorkflowEngine.ts` (720 行)\n\n**核心功能**：\n- 工作流執行協調器\n- 節點依賴解析\n- 條件執行（conditional execution）\n- 級聯停用（cascade disable）\n- 觸發器輪詢機制（Fallback/Sequential）\n- 節點快取（node cache）\n\n**關鍵測試場景**：\n\n#### ✅ 必須測試（Essential）\n1. **基礎執行流程**\n   ```typescript\n   describe(\'WorkflowEngine\', () => {\n     it(\'should execute a simple linear workflow (A → B → C)\', async () => {\n       const workflow = {\n         nodes: [nodeA, nodeB, nodeC],\n         edges: [{ source: \'A\', target: \'B\' }, { source: \'B\', target: \'C\' }]\n       }\n       const engine = new WorkflowEngine(workflow, \'exec-1\', mockEmit)\n       await engine.run()\n       expect(mockEmit).toHaveBeenCalledWith(\'execution:complete\', expect.any(Object))\n     })\n   })\n   ```\n\n2. **依賴解析**\n   ```typescript\n   it(\'should wait for all dependencies before executing node\', async () => {\n     // 測試菱形依賴：A → [B, C] → D\n     // D 必須等 B 和 C 都完成\n   })\n   ```\n\n3. **條件執行（Conditional Execution）**\n   ```typescript\n   it(\'should skip node when all inputs are null\', async () => {\n     // 輸入全為 null → 跳過執行 → 輸出 null\n   })\n   \n   it(\'should continue execution when at least one input is valid\', async () => {\n     // 至少一個輸入有值 → 正常執行\n   })\n   ```\n\n4. **級聯停用（Cascade Disable）**\n   ```typescript\n   it(\'should cascade disable non-data nodes when all upstreams are disabled\', async () => {\n     // 上游全部停用 → 下游自動級聯停用\n   })\n   \n   it(\'should NOT cascade disable data nodes\', async () => {\n     // 資料類節點不參與級聯停用\n   })\n   ```\n\n5. **快取機制**\n   ```typescript\n   it(\'should use cached result when useCachedResult is true\', async () => {\n     const nodeCache = { \'node-1\': { result: \'cached data\' } }\n     const engine = new WorkflowEngine(workflow, \'exec-1\', mockEmit, nodeCache)\n     await engine.run()\n     // 驗證沒有實際執行 executor\n     expect(mockExecutor).not.toHaveBeenCalled()\n   })\n   ```\n\n6. **觸發器模式**\n   ```typescript\n   it(\'should run triggers in fallback mode (stop at first success)\', async () => {\n     // Fallback 模式：第一個成功就停止\n   })\n   \n   it(\'should run triggers in sequential mode (run all)\', async () => {\n     // Sequential 模式：依序執行所有觸發器\n   })\n   ```\n\n7. **錯誤處理**\n   ```typescript\n   it(\'should mark execution as failed when node throws error\', async () => {\n     // 節點執行失敗 → execution.status = \'failed\'\n   })\n   \n   it(\'should emit execution:failed event on error\', async () => {\n     // 驗證錯誤事件發送\n   })\n   ```\n\n#### ⚠️ 邊界條件測試\n- 空工作流（沒有節點）\n- 沒有起始節點（所有節點都有輸入）\n- 循環依賴檢測\n- 孤立節點（沒有連線）\n\n**測試難點**：\n- 需要 mock `executeNode` 函數\n- 需要驗證 emit 事件的順序和內容\n- 非同步並行執行的測試\n\n---\n\n### 3.2 Executors 測試需求（優先級：🔴 Critical）\n\n**現有 Executors**：14 個\n- `youtube-monitor`, `notebooklm`, `gemini`, `send-telegram`, `youtube-subtitle`, `youtube-thumbnail`, `youtube-recent-videos`, `segment-mining`, `script-generator`, `skill5`, `fact-check-95`, `line-breaker`, `write-collection`, `execution-logger`\n\n#### 優先級分類\n\n**🔴 Tier 1（最優先，3 個）**：\n1. **gemini** - AI 核心，使用頻率高\n2. **send-telegram** - 通知出口，失敗影響大\n3. **youtube-monitor** - 觸發器，狀態管理複雜\n\n**🟡 Tier 2（高優先級，4 個）**：\n4. **notebooklm** - 瀏覽器自動化，複雜度高\n5. **youtube-subtitle** - 多重 fallback 邏輯\n6. **segment-mining** - 業務邏輯核心\n7. **script-generator** - 業務邏輯核心\n\n**🟢 Tier 3（可延後，7 個）**：\n其餘 Executors\n\n---\n\n#### 3.2.1 Gemini Executor 測試範例\n\n**檔案**：`server/src/executors/gemini.ts` (228 行)\n\n**核心功能**：\n- 支援 API Key 和 GCP Vertex AI 兩種模式\n- 429 錯誤自動重試機制\n- Temperature、MaxTokens 參數配置\n\n**測試範例**：\n\n```typescript\n// server/src/executors/__tests__/gemini.test.ts\n\nimport { describe, it, expect, vi, beforeEach } from \'vitest\'\nimport { executeGemini } from \'../gemini\'\nimport { GoogleGenerativeAI } from \'@google/generative-ai\'\nimport { VertexAI } from \'@google-cloud/vertexai\'\n\n// Mock 外部依賴\nvi.mock(\'@google/generative-ai\')\nvi.mock(\'@google-cloud/vertexai\')\nvi.mock(\'fs\', () => ({\n  existsSync: vi.fn(),\n  readFileSync: vi.fn()\n}))\n\ndescribe(\'Gemini Executor\', () => {\n  const mockEmit = vi.fn()\n\n  beforeEach(() => {\n    vi.clearAllMocks()\n  })\n\n  describe(\'API Key Mode\', () => {\n    it(\'should call Gemini API with correct parameters\', async () => {\n      // Arrange\n      const mockGenerateContent = vi.fn().mockResolvedValue({\n        response: { text: () => \'AI response\' }\n      })\n      \n      vi.mocked(GoogleGenerativeAI).mockImplementation(() => ({\n        getGenerativeModel: () => ({\n          generateContent: mockGenerateContent\n        })\n      }))\n\n      process.env.GEMINI_API_KEY = \'test-api-key\'\n\n      const config = {\n        prompt: \'Tell me a joke\',\n        systemPrompt: \'You are a comedian\',\n        model: \'gemini-2.0-flash-exp\',\n        temperature: 0.7,\n        maxTokens: 1024\n      }\n\n      // Act\n      const result = await executeGemini(config, mockEmit)\n\n      // Assert\n      expect(result).toEqual({ result: \'AI response\' })\n      expect(mockGenerateContent).toHaveBeenCalledWith(\'Tell me a joke\')\n      expect(mockEmit).toHaveBeenCalledWith(\'node:log\', \n        expect.objectContaining({ message: expect.stringContaining(\'API Key\') })\n      )\n    })\n\n    it(\'should retry on 429 error (rate limit)\', async () => {\n      // 模擬 429 錯誤，第 2 次成功\n      const mockGenerateContent = vi.fn()\n        .mockRejectedValueOnce(new Error(\'429 Too Many Requests\'))\n        .mockResolvedValueOnce({\n          response: { text: () => \'Success after retry\' }\n        })\n\n      vi.mocked(GoogleGenerativeAI).mockImplementation(() => ({\n        getGenerativeModel: () => ({\n          generateContent: mockGenerateContent\n        })\n      }))\n\n      process.env.GEMINI_API_KEY = \'test-key\'\n\n      const result = await executeGemini({ prompt: \'test\' }, mockEmit)\n\n      expect(result.result).toBe(\'Success after retry\')\n      expect(mockGenerateContent).toHaveBeenCalledTimes(2)\n      expect(mockEmit).toHaveBeenCalledWith(\'node:log\', \n        expect.objectContaining({ message: expect.stringContaining(\'重試\') })\n      )\n    })\n\n    it(\'should throw error after max retries\', async () => {\n      const mockGenerateContent = vi.fn()\n        .mockRejectedValue(new Error(\'429 Too Many Requests\'))\n\n      vi.mocked(GoogleGenerativeAI).mockImplementation(() => ({\n        getGenerativeModel: () => ({ generateContent: mockGenerateContent })\n      }))\n\n      process.env.GEMINI_API_KEY = \'test-key\'\n\n      await expect(\n        executeGemini({ prompt: \'test\' }, mockEmit)\n      ).rejects.toThrow(\'429\')\n\n      expect(mockGenerateContent).toHaveBeenCalledTimes(3) // MAX_RETRIES = 3\n    })\n  })\n\n  describe(\'GCP Vertex AI Mode\', () => {\n    it(\'should use GCP credentials when available\', async () => {\n      // 測試 GCP 模式\n    })\n  })\n\n  describe(\'Error Handling\', () => {\n    it(\'should throw error when prompt is missing\', async () => {\n      await expect(\n        executeGemini({ prompt: \'\' }, mockEmit)\n      ).rejects.toThrow(\'prompt is required\')\n    })\n\n    it(\'should handle empty response\', async () => {\n      const mockGenerateContent = vi.fn().mockResolvedValue({\n        response: { text: () => \'\' }\n      })\n      \n      vi.mocked(GoogleGenerativeAI).mockImplementation(() => ({\n        getGenerativeModel: () => ({ generateContent: mockGenerateContent })\n      }))\n\n      process.env.GEMINI_API_KEY = \'test-key\'\n\n      await expect(\n        executeGemini({ prompt: \'test\' }, mockEmit)\n      ).rejects.toThrow(\'未返回有效回應\')\n    })\n  })\n\n  describe(\'Configuration Validation\', () => {\n    it(\'should use default values for optional parameters\', async () => {\n      // 測試預設值：model, temperature, maxTokens\n    })\n  })\n})\n```\n\n**測試覆蓋率目標**：\n- ✅ 正常流程（API Key / GCP 兩種模式）\n- ✅ 重試機制（429 錯誤）\n- ✅ 錯誤處理（參數驗證、空回應、500 錯誤）\n- ✅ 配置驗證（預設值）\n\n---\n\n#### 3.2.2 YouTube Monitor Executor 測試範例\n\n**檔案**：`server/src/executors/youtube-monitor.ts` (213 行)\n\n**核心功能**：\n- RSS feed 解析\n- 狀態持久化（記住已處理的影片 ID）\n- Channel ID 轉換（@handle → channel ID）\n\n**關鍵測試場景**：\n\n```typescript\n// server/src/executors/__tests__/youtube-monitor.test.ts\n\nimport { describe, it, expect, vi, beforeEach, afterEach } from \'vitest\'\nimport { executeYouTubeMonitor } from \'../youtube-monitor\'\nimport * as youtubeUtils from \'../../utils/youtube-utils\'\nimport * as fs from \'fs\'\n\nvi.mock(\'../../utils/youtube-utils\')\nvi.mock(\'fs\')\n\ndescribe(\'YouTube Monitor Executor\', () => {\n  const mockEmit = vi.fn()\n  const STATE_FILE_PATH = expect.stringContaining(\'youtube-monitor-state.json\')\n\n  beforeEach(() => {\n    vi.clearAllMocks()\n  })\n\n  describe(\'First Run (No State)\', () => {\n    it(\'should return the latest video when no previous state exists\', async () => {\n      // Mock: 沒有 state 檔案\n      vi.mocked(fs.existsSync).mockReturnValue(false)\n      \n      // Mock: 頻道有 1 個影片\n      vi.mocked(youtubeUtils.getRecentVideos).mockResolvedValue([\n        {\n          videoId: \'abc123\',\n          title: \'Test Video\',\n          url: \'https://youtube.com/watch?v=abc123\',\n          publishedAt: \'2024-01-01T00:00:00Z\'\n        }\n      ])\n\n      const config = {\n        channels: JSON.stringify([\n          { id: \'UC123\', name: \'Test Channel\' }\n        ]),\n        monitorType: \'new_video\'\n      }\n\n      const result = await executeYouTubeMonitor(config, mockEmit)\n\n      expect(result).toMatchObject({\n        video: expect.objectContaining({ videoId: \'abc123\' }),\n        title: \'Test Video\',\n        url: \'https://youtube.com/watch?v=abc123\'\n      })\n\n      // 驗證 state 已儲存\n      expect(fs.writeFileSync).toHaveBeenCalledWith(\n        STATE_FILE_PATH,\n        expect.stringContaining(\'abc123\')\n      )\n    })\n  })\n\n  describe(\'Subsequent Runs (With State)\', () => {\n    it(\'should return null when video is already processed\', async () => {\n      // Mock: state 檔案存在，記錄了 videoId = \'abc123\'\n      vi.mocked(fs.existsSync).mockReturnValue(true)\n      vi.mocked(fs.readFileSync).mockReturnValue(\n        JSON.stringify({ lastVideoId: \'abc123\' })\n      )\n\n      // Mock: 頻道最新影片仍是 \'abc123\'（沒有新影片）\n      vi.mocked(youtubeUtils.getRecentVideos).mockResolvedValue([\n        { videoId: \'abc123\', title: \'Same Video\', url: \'...\', publishedAt: \'...\' }\n      ])\n\n      const config = {\n        channels: JSON.stringify([{ id: \'UC123\', name: \'Test Channel\' }]),\n        monitorType: \'new_video\'\n      }\n\n      const result = await executeYouTubeMonitor(config, mockEmit)\n\n      // 驗證：沒有新影片，返回 null\n      expect(result).toMatchObject({\n        video: null,\n        title: null,\n        duration: null,\n        subscribers: null\n      })\n\n      expect(mockEmit).toHaveBeenCalledWith(\'node:log\',\n        expect.objectContaining({ message: expect.stringContaining(\'已處理過\') })\n      )\n    })\n\n    it(\'should return new video when different from state\', async () => {\n      vi.mocked(fs.existsSync).mockReturnValue(true)\n      vi.mocked(fs.readFileSync).mockReturnValue(\n        JSON.stringify({ lastVideoId: \'old123\' })\n      )\n\n      vi.mocked(youtubeUtils.getRecentVideos).mockResolvedValue([\n        { videoId: \'new456\', title: \'New Video\', url: \'...\', publishedAt: \'...\' }\n      ])\n\n      const config = {\n        channels: JSON.stringify([{ id: \'UC123\', name: \'Test\' }])\n      }\n\n      const result = await executeYouTubeMonitor(config, mockEmit)\n\n      expect(result.video.videoId).toBe(\'new456\')\n      \n      // 驗證：新的 videoId 已儲存\n      expect(fs.writeFileSync).toHaveBeenCalledWith(\n        STATE_FILE_PATH,\n        expect.stringContaining(\'new456\')\n      )\n    })\n  })\n\n  describe(\'Multiple Channels\', () => {\n    it(\'should return the latest video across all channels\', async () => {\n      vi.mocked(fs.existsSync).mockReturnValue(false)\n\n      vi.mocked(youtubeUtils.getRecentVideos)\n        .mockResolvedValueOnce([\n          { videoId: \'vid1\', publishedAt: \'2024-01-01T00:00:00Z\', ... }\n        ])\n        .mockResolvedValueOnce([\n          { videoId: \'vid2\', publishedAt: \'2024-01-02T00:00:00Z\', ... }\n        ])\n\n      const config = {\n        channels: JSON.stringify([\n          { id: \'UC123\', name: \'Channel 1\' },\n          { id: \'UC456\', name: \'Channel 2\' }\n        ])\n      }\n\n      const result = await executeYouTubeMonitor(config, mockEmit)\n\n      // 應該返回最新的影片（vid2，時間較晚）\n      expect(result.video.videoId).toBe(\'vid2\')\n    })\n  })\n\n  describe(\'Error Handling\', () => {\n    it(\'should handle empty channels list\', async () => {\n      const config = { channels: \'[]\' }\n      const result = await executeYouTubeMonitor(config, mockEmit)\n\n      expect(result.video).toBeNull()\n      expect(mockEmit).toHaveBeenCalledWith(\'node:log\',\n        expect.objectContaining({ message: expect.stringContaining(\'沒有設定監控頻道\') })\n      )\n    })\n\n    it(\'should handle invalid JSON in channels\', async () => {\n      const config = { channels: \'invalid json\' }\n\n      await expect(\n        executeYouTubeMonitor(config, mockEmit)\n      ).rejects.toThrow(\'頻道列表格式錯誤\')\n    })\n\n    it(\'should continue when one channel fails\', async () => {\n      vi.mocked(youtubeUtils.getRecentVideos)\n        .mockRejectedValueOnce(new Error(\'Channel 1 failed\'))\n        .mockResolvedValueOnce([\n          { videoId: \'vid2\', ... }\n        ])\n\n      const config = {\n        channels: JSON.stringify([\n          { id: \'UC123\', name: \'Bad Channel\' },\n          { id: \'UC456\', name: \'Good Channel\' }\n        ])\n      }\n\n      const result = await executeYouTubeMonitor(config, mockEmit)\n\n      // 應該返回第二個頻道的影片\n      expect(result.video.videoId).toBe(\'vid2\')\n    })\n  })\n\n  describe(\'State File Management\', () => {\n    it(\'should create data directory if not exists\', async () => {\n      vi.mocked(fs.existsSync).mockReturnValue(false)\n      vi.mocked(youtubeUtils.getRecentVideos).mockResolvedValue([\n        { videoId: \'vid1\', ... }\n      ])\n\n      const config = {\n        channels: JSON.stringify([{ id: \'UC123\', name: \'Test\' }])\n      }\n\n      await executeYouTubeMonitor(config, mockEmit)\n\n      expect(fs.mkdirSync).toHaveBeenCalledWith(\n        expect.stringContaining(\'data\'),\n        { recursive: true }\n      )\n    })\n  })\n})\n```\n\n---\n\n#### 3.2.3 Send Telegram Executor 測試範例\n\n**檔案**：`server/src/executors/send-telegram.ts` (365 行)\n\n**核心功能**：\n- 發送文字/圖片訊息\n- Inline Keyboard（互動按鈕）\n- Markdown 解析失敗自動重試\n- 本地檔案上傳（FormData）\n\n**測試範例**：\n\n```typescript\n// server/src/executors/__tests__/send-telegram.test.ts\n\nimport { describe, it, expect, vi, beforeEach } from \'vitest\'\nimport { executeSendTelegram } from \'../send-telegram\'\n\n// Mock fetch\nglobal.fetch = vi.fn()\n\ndescribe(\'Send Telegram Executor\', () => {\n  const mockEmit = vi.fn()\n\n  beforeEach(() => {\n    vi.clearAllMocks()\n  })\n\n  describe(\'Text Message\', () => {\n    it(\'should send text message successfully\', async () => {\n      vi.mocked(fetch).mockResolvedValue({\n        json: async () => ({ ok: true, result: { message_id: 123 } })\n      } as Response)\n\n      const config = {\n        bot_token: \'test-token\',\n        chat_id: \'-100123456\',\n        message: \'Hello World\'\n      }\n\n      const result = await executeSendTelegram(config, mockEmit)\n\n      expect(result).toEqual({\n        success: true,\n        message_id: \'123\'\n      })\n\n      expect(fetch).toHaveBeenCalledWith(\n        \'https://api.telegram.org/bottest-token/sendMessage\',\n        expect.objectContaining({\n          method: \'POST\',\n          headers: { \'Content-Type\': \'application/json\' },\n          body: expect.stringContaining(\'Hello World\')\n        })\n      )\n    })\n\n    it(\'should support Markdown parse mode\', async () => {\n      vi.mocked(fetch).mockResolvedValue({\n        json: async () => ({ ok: true, result: { message_id: 123 } })\n      } as Response)\n\n      const config = {\n        bot_token: \'test-token\',\n        chat_id: \'-100123456\',\n        message: \'**Bold** text\',\n        parse_mode: \'Markdown\'\n      }\n\n      await executeSendTelegram(config, mockEmit)\n\n      expect(fetch).toHaveBeenCalledWith(\n        expect.any(String),\n        expect.objectContaining({\n          body: expect.stringContaining(\'"parse_mode":"Markdown"\')\n        })\n      )\n    })\n  })\n\n  describe(\'Photo Message\', () => {\n    it(\'should send photo with caption\', async () => {\n      vi.mocked(fetch).mockResolvedValue({\n        json: async () => ({ ok: true, result: { message_id: 456 } })\n      } as Response)\n\n      const config = {\n        bot_token: \'test-token\',\n        chat_id: \'-100123456\',\n        message: \'Photo caption\',\n        photo: \'https://example.com/image.jpg\'\n      }\n\n      const result = await executeSendTelegram(config, mockEmit)\n\n      expect(result.success).toBe(true)\n      expect(fetch).toHaveBeenCalledWith(\n        \'https://api.telegram.org/bottest-token/sendPhoto\',\n        expect.objectContaining({\n          body: expect.stringContaining(\'https://example.com/image.jpg\')\n        })\n      )\n    })\n  })\n\n  describe(\'Inline Keyboard\', () => {\n    it(\'should add tags as inline buttons\', async () => {\n      vi.mocked(fetch).mockResolvedValue({\n        json: async () => ({ ok: true, result: { message_id: 789 } })\n      } as Response)\n\n      const config = {\n        bots: JSON.stringify([{ name: \'Bot1\', token: \'token1\' }]),\n        chats: JSON.stringify([{ name: \'Chat1\', chat_id: \'-100123\' }]),\n        selectedBotIdx: 0,\n        selectedChatIdx: 0,\n        message: \'Test\',\n        tags: JSON.stringify([\n          { name: \'✅ 通過\', code: \'approve\', responseMessage: \'已通過\' }\n        ]),\n        selectedTags: JSON.stringify([\'✅ 通過\']),\n        recordCollectionId: \'collection-1\'\n      }\n\n      await executeSendTelegram(config, mockEmit)\n\n      expect(fetch).toHaveBeenCalledWith(\n        expect.any(String),\n        expect.objectContaining({\n          body: expect.stringContaining(\'inline_keyboard\')\n        })\n      )\n    })\n\n    it(\'should add YouTube link button\', async () => {\n      vi.mocked(fetch).mockResolvedValue({\n        json: async () => ({ ok: true, result: { message_id: 999 } })\n      } as Response)\n\n      const config = {\n        bot_token: \'test-token\',\n        chat_id: \'-100123\',\n        message: \'Check this video\',\n        youtubeUrl: \'https://youtube.com/watch?v=abc123\'\n      }\n\n      await executeSendTelegram(config, mockEmit)\n\n      const callBody = vi.mocked(fetch).mock.calls[0][1]?.body as string\n      expect(callBody).toContain(\'打開影片\')\n      expect(callBody).toContain(\'https://youtube.com/watch?v=abc123\')\n    })\n  })\n\n  describe(\'Error Handling\', () => {\n    it(\'should throw error when message is empty\', async () => {\n      const config = {\n        bot_token: \'test-token\',\n        chat_id: \'-100123\',\n        message: \'\'\n      }\n\n      await expect(\n        executeSendTelegram(config, mockEmit)\n      ).rejects.toThrow(\'訊息內容不能為空\')\n    })\n\n    it(\'should retry without parse_mode on Markdown error\', async () => {\n      // 第一次呼叫：Markdown 解析失敗\n      vi.mocked(fetch).mockResolvedValueOnce({\n        json: async () => ({ \n          ok: false, \n          description: \'Can\\\'t parse entities\' \n        })\n      } as Response)\n\n      // 第二次呼叫：純文字成功\n      vi.mocked(fetch).mockResolvedValueOnce({\n        json: async () => ({ ok: true, result: { message_id: 111 } })\n      } as Response)\n\n      const config = {\n        bot_token: \'test-token\',\n        chat_id: \'-100123\',\n        message: \'Text with [invalid markdown\',\n        parse_mode: \'Markdown\'\n      }\n\n      const result = await executeSendTelegram(config, mockEmit)\n\n      expect(result.success).toBe(true)\n      expect(fetch).toHaveBeenCalledTimes(2)\n      expect(mockEmit).toHaveBeenCalledWith(\'node:log\',\n        expect.objectContaining({ message: expect.stringContaining(\'改用純文字\') })\n      )\n    })\n\n    it(\'should throw error on API failure\', async () => {\n      vi.mocked(fetch).mockResolvedValue({\n        json: async () => ({ \n          ok: false, \n          description: \'Bot was blocked by user\' \n        })\n      } as Response)\n\n      const config = {\n        bot_token: \'test-token\',\n        chat_id: \'-100123\',\n        message: \'Test\'\n      }\n\n      await expect(\n        executeSendTelegram(config, mockEmit)\n      ).rejects.toThrow(\'Bot was blocked by user\')\n    })\n  })\n\n  describe(\'Configuration Parsing\', () => {\n    it(\'should parse new format (bots/chats)\', async () => {\n      // 測試新版配置格式\n    })\n\n    it(\'should support legacy format (bot_token/chat_id)\', async () => {\n      // 測試舊版配置格式\n    })\n  })\n})\n```\n\n---\n\n### 3.3 觸發器系統測試（優先級：🟡 High）\n\n**核心模組**：\n- WorkflowEngine 的觸發器輪詢邏輯\n- `youtube-monitor` (已分析)\n- `youtube-recent-videos`\n\n**測試需求**：\n1. Fallback 模式測試\n2. Sequential 模式測試\n3. 狀態持久化測試\n4. 空結果判斷測試（hasTriggerContent）\n\n**測試範例**：\n\n```typescript\n// server/src/execution/__tests__/WorkflowEngine.triggers.test.ts\n\ndescribe(\'WorkflowEngine - Trigger Modes\', () => {\n  describe(\'Fallback Mode\', () => {\n    it(\'should stop at first successful trigger\', async () => {\n      const workflow = {\n        triggerMode: \'fallback\',\n        nodes: [trigger1, trigger2, trigger3, downstreamNode],\n        edges: [...]\n      }\n\n      // Mock: trigger1 失敗，trigger2 成功\n      mockExecutor\n        .mockRejectedValueOnce(new Error(\'Trigger 1 failed\'))\n        .mockResolvedValueOnce({ data: \'trigger 2 success\' })\n\n      const engine = new WorkflowEngine(workflow, \'exec-1\', mockEmit)\n      await engine.run()\n\n      expect(mockExecutor).toHaveBeenCalledTimes(2) // 只執行 2 個觸發器\n      expect(mockEmit).toHaveBeenCalledWith(\'node:log\',\n        expect.objectContaining({ message: expect.stringContaining(\'觸發器 2 成功\') })\n      )\n    })\n\n    it(\'should try all triggers when all return empty\', async () => {\n      // Mock: 所有觸發器都返回空結果\n      mockExecutor.mockResolvedValue({ data: null })\n\n      const engine = new WorkflowEngine(workflow, \'exec-1\', mockEmit)\n      await engine.run()\n\n      expect(mockExecutor).toHaveBeenCalledTimes(3) // 嘗試所有觸發器\n      expect(mockEmit).toHaveBeenCalledWith(\'node:log\',\n        expect.objectContaining({ message: expect.stringContaining(\'所有觸發器都暫無內容\') })\n      )\n    })\n  })\n\n  describe(\'Sequential Mode\', () => {\n    it(\'should run all triggers regardless of result\', async () => {\n      const workflow = {\n        triggerMode: \'sequential\',\n        nodes: [trigger1, trigger2, trigger3, downstreamNode],\n        edges: [...]\n      }\n\n      mockExecutor\n        .mockResolvedValueOnce({ data: \'trigger 1\' })\n        .mockResolvedValueOnce({ data: \'trigger 2\' })\n        .mockRejectedValueOnce(new Error(\'Trigger 3 failed\'))\n\n      const engine = new WorkflowEngine(workflow, \'exec-1\', mockEmit)\n      await engine.run()\n\n      expect(mockExecutor).toHaveBeenCalledTimes(3) // 所有觸發器都執行\n    })\n  })\n\n  describe(\'hasTriggerContent\', () => {\n    it(\'should detect empty results correctly\', () => {\n      const engine = new WorkflowEngine(workflow, \'exec-1\', mockEmit)\n\n      expect(engine[\'hasTriggerContent\'](null)).toBe(false)\n      expect(engine[\'hasTriggerContent\'](undefined)).toBe(false)\n      expect(engine[\'hasTriggerContent\'](\'\')).toBe(false)\n      expect(engine[\'hasTriggerContent\']([])).toBe(false)\n      expect(engine[\'hasTriggerContent\']({})).toBe(false)\n      expect(engine[\'hasTriggerContent\']({ video: null, duration: null })).toBe(false)\n      expect(engine[\'hasTriggerContent\']({ video: \'abc\' })).toBe(true)\n    })\n  })\n})\n```\n\n---\n\n### 3.4 前端組件測試（優先級：🟡 High）\n\n**核心組件**：\n- `Editor.vue` - 工作流編輯器\n- `CustomNode.vue` - 自定義節點渲染\n- `*Config.vue` - 8 個節點配置組件\n\n**測試框架**：`@vue/test-utils` + Vitest\n\n**測試範例**：\n\n```typescript\n// src/components/__tests__/CustomNode.test.ts\n\nimport { describe, it, expect } from \'vitest\'\nimport { mount } from \'@vue/test-utils\'\nimport CustomNode from \'../CustomNode.vue\'\n\ndescribe(\'CustomNode Component\', () => {\n  it(\'should render node label\', () => {\n    const wrapper = mount(CustomNode, {\n      props: {\n        id: \'node-1\',\n        data: {\n          label: \'Test Node\',\n          nodeType: \'gemini\'\n        }\n      }\n    })\n\n    expect(wrapper.text()).toContain(\'Test Node\')\n  })\n\n  it(\'should show disabled badge when node is disabled\', () => {\n    const wrapper = mount(CustomNode, {\n      props: {\n        id: \'node-1\',\n        data: {\n          label: \'Disabled Node\',\n          disabled: true\n        }\n      }\n    })\n\n    expect(wrapper.html()).toContain(\'已停用\')\n  })\n\n  it(\'should emit open-config when double clicked\', async () => {\n    const wrapper = mount(CustomNode, {\n      props: {\n        id: \'node-1\',\n        data: { label: \'Test\' }\n      }\n    })\n\n    await wrapper.trigger(\'dblclick\')\n\n    expect(wrapper.emitted(\'open-config\')).toBeTruthy()\n  })\n})\n```\n\n**Pinia Store 測試**：\n\n```typescript\n// src/stores/__tests__/workflow.test.ts\n\nimport { describe, it, expect, beforeEach } from \'vitest\'\nimport { setActivePinia, createPinia } from \'pinia\'\nimport { useWorkflowStore } from \'../workflow\'\n\ndescribe(\'Workflow Store\', () => {\n  beforeEach(() => {\n    setActivePinia(createPinia())\n    localStorage.clear()\n  })\n\n  it(\'should create new workflow\', () => {\n    const store = useWorkflowStore()\n\n    const workflow = store.createWorkflow(\'Test Workflow\', \'Test description\')\n\n    expect(workflow.name).toBe(\'Test Workflow\')\n    expect(workflow.description).toBe(\'Test description\')\n    expect(workflow.active).toBe(false)\n    expect(store.workflows).toHaveLength(1)\n  })\n\n  it(\'should save workflow with nodes and edges\', () => {\n    const store = useWorkflowStore()\n    const workflow = store.createWorkflow(\'Test\')\n\n    const nodes = [{ id: \'node-1\', data: { label: \'Node 1\' } }]\n    const edges = [{ id: \'edge-1\', source: \'node-1\', target: \'node-2\' }]\n\n    store.saveWorkflow(workflow.id, nodes, edges)\n\n    const saved = store.getWorkflow(workflow.id)\n    expect(saved?.nodes).toEqual(nodes)\n    expect(saved?.edges).toEqual(edges)\n  })\n\n  it(\'should toggle workflow active state\', () => {\n    const store = useWorkflowStore()\n    const workflow = store.createWorkflow(\'Test\')\n\n    expect(workflow.active).toBe(false)\n\n    store.toggleActive(workflow.id)\n    expect(store.getWorkflow(workflow.id)?.active).toBe(true)\n\n    store.toggleActive(workflow.id)\n    expect(store.getWorkflow(workflow.id)?.active).toBe(false)\n  })\n\n  it(\'should export workflow as JSON\', () => {\n    const store = useWorkflowStore()\n    const workflow = store.createWorkflow(\'Export Test\')\n    \n    // Mock document.createElement\n    const mockLink = { click: vi.fn(), href: \'\', download: \'\' }\n    vi.spyOn(document, \'createElement\').mockReturnValue(mockLink as any)\n\n    store.exportWorkflow(workflow.id)\n\n    expect(mockLink.download).toContain(\'Export_Test.json\')\n  })\n})\n```\n\n---\n\n### 3.5 API Endpoints 測試（優先級：🟡 High）\n\n**檔案**：`server/src/index.ts`\n\n**關鍵 Endpoints**：\n- `POST /api/workflow/run` - 執行工作流\n- `POST /api/execute` - 執行單個節點\n- `POST /api/workflows/sync` - 同步工作流\n- `GET /api/youtube/recent-videos` - 獲取最近影片\n\n**測試範例**：\n\n```typescript\n// server/src/__tests__/api.integration.test.ts\n\nimport { describe, it, expect, beforeAll, afterAll } from \'vitest\'\nimport request from \'supertest\'\nimport express from \'express\'\n// Import your app instance\n\ndescribe(\'API Integration Tests\', () => {\n  describe(\'POST /api/workflow/run\', () => {\n    it(\'should start workflow execution\', async () => {\n      const workflow = {\n        id: \'wf-test\',\n        nodes: [\n          { id: \'node-1\', type: \'manual-trigger\', data: { payload: \'{}\' } }\n        ],\n        edges: []\n      }\n\n      const response = await request(app)\n        .post(\'/api/workflow/run\')\n        .send({ workflow, socketId: \'test-socket\' })\n        .expect(200)\n\n      expect(response.body).toMatchObject({\n        ok: true,\n        executionId: expect.stringContaining(\'exec-\'),\n        message: \'工作流已開始執行\'\n      })\n    })\n\n    it(\'should return 400 when workflow is missing\', async () => {\n      const response = await request(app)\n        .post(\'/api/workflow/run\')\n        .send({})\n        .expect(400)\n\n      expect(response.body.error).toBe(\'workflow is required\')\n    })\n  })\n\n  describe(\'POST /api/execute\', () => {\n    it(\'should execute single node\', async () => {\n      const response = await request(app)\n        .post(\'/api/execute\')\n        .send({\n          nodeType: \'manual-trigger\',\n          config: { payload: \'{"test": true}\' },\n          socketId: \'test-socket\'\n        })\n        .expect(200)\n\n      expect(response.body).toMatchObject({\n        ok: true,\n        result: expect.objectContaining({\n          timestamp: expect.any(String)\n        })\n      })\n    })\n\n    it(\'should return 400 when nodeType is missing\', async () => {\n      await request(app)\n        .post(\'/api/execute\')\n        .send({ config: {} })\n        .expect(400)\n    })\n  })\n\n  describe(\'GET /api/health\', () => {\n    it(\'should return health status\', async () => {\n      const response = await request(app)\n        .get(\'/api/health\')\n        .expect(200)\n\n      expect(response.body).toEqual({\n        status: \'ok\',\n        version: \'1.0.0\'\n      })\n    })\n  })\n})\n```\n\n---\n\n## 4. 測試缺口清單（按優先級排序）\n\n### 🔴 Critical（必須，短期內完成）\n\n| 測試項目 | 預估工時 | 風險等級 | 建議完成時間 |\n|---------|---------|---------|-------------|\n| **WorkflowEngine 核心流程** | 2 天 | 🔴 極高 | Week 1 |\n| - 基礎執行流程（A → B → C） | 4 小時 | | |\n| - 依賴解析與並行執行 | 4 小時 | | |\n| - 條件執行（null 判斷） | 2 小時 | | |\n| - 級聯停用 | 2 小時 | | |\n| - 節點快取 | 2 小時 | | |\n| **Gemini Executor** | 1 天 | 🔴 高 | Week 1 |\n| - API Key 模式 | 2 小時 | | |\n| - GCP 模式 | 2 小時 | | |\n| - 429 重試機制 | 2 小時 | | |\n| - 錯誤處理 | 2 小時 | | |\n| **Send Telegram Executor** | 1 天 | 🔴 高 | Week 1 |\n| - 文字/圖片發送 | 3 小時 | | |\n| - Inline Keyboard | 2 小時 | | |\n| - Markdown 重試 | 2 小時 | | |\n| - 錯誤處理 | 1 小時 | | |\n| **YouTube Monitor Executor** | 1 天 | 🔴 高 | Week 2 |\n| - 狀態持久化 | 2 小時 | | |\n| - 新影片檢測 | 2 小時 | | |\n| - 多頻道排序 | 2 小時 | | |\n| - 錯誤處理 | 2 小時 | | |\n\n**小計**：5 天（1 週）\n\n---\n\n### 🟡 High（重要，中期完成）\n\n| 測試項目 | 預估工時 | 優先級 | 建議完成時間 |\n|---------|---------|--------|-------------|\n| **觸發器系統測試** | 1 天 | 🟡 | Week 2 |\n| - Fallback 模式 | 3 小時 | | |\n| - Sequential 模式 | 2 小時 | | |\n| - hasTriggerContent 邏輯 | 2 小時 | | |\n| **NotebookLM Executor** | 2 天 | 🟡 | Week 3 |\n| - 瀏覽器自動化模擬 | 4 小時 | | |\n| - URL mapping 邏輯 | 2 小時 | | |\n| - 超時處理 | 2 小時 | | |\n| - 錯誤處理 | 2 小時 | | |\n| **API Endpoints 整合測試** | 1 天 | 🟡 | Week 2 |\n| - `/api/workflow/run` | 2 小時 | | |\n| - `/api/execute` | 2 小時 | | |\n| - `/api/workflows/sync` | 2 小時 | | |\n| - Error handling | 2 小時 | | |\n| **前端 Store 測試** | 1 天 | 🟡 | Week 3 |\n| - Workflow Store | 3 小時 | | |\n| - Execution Store | 3 小時 | | |\n| - Collection Store | 2 小時 | | |\n| **YouTube Utilities** | 0.5 天 | 🟡 | Week 3 |\n| - extractChannelId | 1 小時 | | |\n| - getRecentVideos | 2 小時 | | |\n| - RSS 解析 | 1 小時 | | |\n\n**小計**：5.5 天（約 1 週）\n\n---\n\n### 🟢 Nice-to-have（建議，長期完成）\n\n| 測試項目 | 預估工時 | 優先級 | 建議完成時間 |\n|---------|---------|--------|-------------|\n| **其他 Executors** | 3 天 | 🟢 | Week 4-5 |\n| - segment-mining | 4 小時 | | |\n| - script-generator | 4 小時 | | |\n| - youtube-subtitle | 4 小時 | | |\n| - youtube-thumbnail | 2 小時 | | |\n| - skill5 | 4 小時 | | |\n| - execution-logger | 2 小時 | | |\n| - write-collection | 2 小時 | | |\n| **前端組件測試** | 2 天 | 🟢 | Week 5 |\n| - CustomNode.vue | 3 小時 | | |\n| - Editor.vue | 4 小時 | | |\n| - Config 組件（8 個） | 8 小時 | | |\n| **E2E 測試** | 2 天 | 🟢 | Week 6 |\n| - 完整工作流執行 | 4 小時 | | |\n| - 觸發器自動輪詢 | 4 小時 | | |\n| - UI 互動測試 | 4 小時 | | |\n\n**小計**：7 天（約 1.5 週）\n\n---\n\n**總計預估工時**：17.5 天（約 3.5 週 full-time）\n\n---\n\n## 5. 測試策略建議\n\n### 5.1 短期目標（Week 1-2，10 天）\n\n**目標**：建立測試基礎設施 + 覆蓋核心功能\n\n**Action Items**：\n\n1. **建立測試框架**（Day 1）\n   ```bash\n   # 安裝依賴\n   npm install -D vitest @vitest/ui c8\n   cd server && npm install -D vitest @types/node\n   ```\n\n   ```typescript\n   // vitest.config.ts（前端）\n   import { defineConfig } from \'vitest/config\'\n   import vue from \'@vitejs/plugin-vue\'\n\n   export default defineConfig({\n     plugins: [vue()],\n     test: {\n       globals: true,\n       environment: \'jsdom\',\n       setupFiles: \'./src/__tests__/setup.ts\',\n       coverage: {\n         provider: \'c8\',\n         reporter: [\'text\', \'html\'],\n         exclude: [\'node_modules/\', \'src/__tests__/\']\n       }\n     }\n   })\n   ```\n\n   ```typescript\n   // server/vitest.config.ts（後端）\n   import { defineConfig } from \'vitest/config\'\n\n   export default defineConfig({\n     test: {\n       globals: true,\n       environment: \'node\',\n       coverage: {\n         provider: \'c8\',\n         reporter: [\'text\', \'lcov\'],\n         exclude: [\'node_modules/\', \'src/__tests__/\']\n       }\n     }\n   })\n   ```\n\n2. **WorkflowEngine 測試**（Day 2-3）\n   - 建立 `server/src/execution/__tests__/WorkflowEngine.test.ts`\n   - 覆蓋基礎流程、依賴解析、條件執行、級聯停用\n   - **目標覆蓋率**：70%+\n\n3. **Gemini Executor 測試**（Day 4）\n   - 建立 `server/src/executors/__tests__/gemini.test.ts`\n   - Mock Google AI SDK\n   - **目標覆蓋率**：80%+\n\n4. **Send Telegram Executor 測試**（Day 5）\n   - 建立 `server/src/executors/__tests__/send-telegram.test.ts`\n   - Mock fetch API\n   - **目標覆蓋率**：75%+\n\n5. **YouTube Monitor Executor 測試**（Day 6）\n   - 建立 `server/src/executors/__tests__/youtube-monitor.test.ts`\n   - Mock fs 和 youtube-utils\n   - **目標覆蓋率**：80%+\n\n6. **觸發器系統測試**（Day 7）\n   - 擴展 WorkflowEngine.test.ts\n   - 測試 Fallback/Sequential 模式\n\n7. **API Integration 測試**（Day 8）\n   - 建立 `server/src/__tests__/api.integration.test.ts`\n   - 使用 supertest\n   - **目標覆蓋率**：60%+\n\n8. **前端 Store 測試**（Day 9-10）\n   - 測試 workflow.ts, execution.ts, collection.ts\n   - **目標覆蓋率**：70%+\n\n**Week 1-2 結束時目標**：\n- ✅ 測試框架完整設置\n- ✅ 核心引擎測試覆蓋率 70%+\n- ✅ 3 個高頻 Executor 測試覆蓋率 75%+\n- ✅ CI 可以跑測試（GitHub Actions）\n\n---\n\n### 5.2 中期目標（Week 3-4，10 天）\n\n**目標**：擴展測試覆蓋至其他模組\n\n**Action Items**：\n\n1. **NotebookLM Executor 測試**（Day 11-12）\n   - Mock Patchright 瀏覽器操作\n   - 測試 URL mapping 和錯誤處理\n\n2. **YouTube Utilities 測試**（Day 13）\n   - 測試 RSS 解析、Channel ID 提取\n\n3. **其他 Executors 測試**（Day 14-17）\n   - segment-mining, script-generator, youtube-subtitle, youtube-thumbnail\n   - 優先測試業務邏輯核心\n\n4. **前端組件測試**（Day 18-20）\n   - CustomNode.vue, Config 組件\n   - 使用 @vue/test-utils\n\n**Week 3-4 結束時目標**：\n- ✅ 7 個 Executors 有測試覆蓋\n- ✅ 前端組件測試覆蓋率 50%+\n- ✅ 整體測試覆蓋率 60%+\n\n---\n\n### 5.3 長期目標（Week 5+）\n\n**目標**：完整測試覆蓋 + E2E 測試\n\n**Action Items**：\n\n1. **剩餘 Executors 測試**\n   - 覆蓋所有 14 個 Executors\n\n2. **E2E 測試**\n   - 使用 Playwright\n   - 測試完整工作流執行\n   - 測試 UI 互動\n\n3. **測試優化**\n   - 減少測試執行時間\n   - 提高測試穩定性\n   - 建立測試最佳實踐文件\n\n4. **持續維護**\n   - 新功能必須附帶測試\n   - 保持測試覆蓋率 80%+\n\n---\n\n## 6. CI/CD 流程建議\n\n### 6.1 GitHub Actions 配置\n\n**建立檔案**：`.github/workflows/test.yml`\n\n```yaml\nname: Test\n\non:\n  push:\n    branches: [main, develop]\n  pull_request:\n    branches: [main, develop]\n\njobs:\n  test-backend:\n    runs-on: ubuntu-latest\n    \n    steps:\n      - uses: actions/checkout@v4\n      \n      - name: Setup Node.js\n        uses: actions/setup-node@v4\n        with:\n          node-version: \'20\'\n          cache: \'npm\'\n          cache-dependency-path: \'server/package-lock.json\'\n      \n      - name: Install dependencies\n        run: |\n          cd server\n          npm ci\n      \n      - name: Run tests\n        run: |\n          cd server\n          npm test -- --coverage\n      \n      - name: Upload coverage\n        uses: codecov/codecov-action@v3\n        with:\n          files: ./server/coverage/lcov.info\n          flags: backend\n\n  test-frontend:\n    runs-on: ubuntu-latest\n    \n    steps:\n      - uses: actions/checkout@v4\n      \n      - name: Setup Node.js\n        uses: actions/setup-node@v4\n        with:\n          node-version: \'20\'\n          cache: \'npm\'\n      \n      - name: Install dependencies\n        run: npm ci\n      \n      - name: Run tests\n        run: npm test -- --coverage\n      \n      - name: Upload coverage\n        uses: codecov/codecov-action@v3\n        with:\n          files: ./coverage/lcov.info\n          flags: frontend\n```\n\n### 6.2 Pre-commit Hook\n\n**建議使用 Husky + lint-staged**：\n\n```bash\nnpm install -D husky lint-staged\n```\n\n```json\n// package.json\n{\n  "scripts": {\n    "test": "vitest run",\n    "test:watch": "vitest",\n    "prepare": "husky install"\n  },\n  "lint-staged": {\n    "*.ts": ["vitest related --run"]\n  }\n}\n```\n\n```bash\n# .husky/pre-commit\nnpm test\n```\n\n### 6.3 部署前檢查\n\n**建議流程**：\n1. ✅ 所有測試通過\n2. ✅ 測試覆蓋率 > 70%\n3. ✅ 沒有高風險的 console.error\n4. ✅ TypeScript 編譯成功\n\n---\n\n## 7. 測試實作指南（含程式碼範例）\n\n### 7.1 測試檔案結構建議\n\n```\nflowcraft/\n├── server/\n│   └── src/\n│       ├── execution/\n│       │   ├── WorkflowEngine.ts\n│       │   └── __tests__/\n│       │       └── WorkflowEngine.test.ts\n│       ├── executors/\n│       │   ├── gemini.ts\n│       │   ├── send-telegram.ts\n│       │   └── __tests__/\n│       │       ├── gemini.test.ts\n│       │       ├── send-telegram.test.ts\n│       │       └── youtube-monitor.test.ts\n│       └── __tests__/\n│           ├── setup.ts\n│           └── api.integration.test.ts\n└── src/\n    ├── stores/\n    │   ├── workflow.ts\n    │   └── __tests__/\n    │       └── workflow.test.ts\n    └── components/\n        ├── CustomNode.vue\n        └── __tests__/\n            └── CustomNode.test.ts\n```\n\n---\n\n### 7.2 Mock 策略\n\n#### Mock 外部 API\n\n```typescript\n// server/src/__tests__/mocks/google-ai.mock.ts\n\nexport const mockGoogleGenerativeAI = {\n  getGenerativeModel: vi.fn().mockReturnValue({\n    generateContent: vi.fn().mockResolvedValue({\n      response: {\n        text: () => \'Mocked AI response\'\n      }\n    })\n  })\n}\n\n// 使用\nvi.mock(\'@google/generative-ai\', () => ({\n  GoogleGenerativeAI: vi.fn(() => mockGoogleGenerativeAI)\n}))\n```\n\n#### Mock File System\n\n```typescript\n// Mock fs\nvi.mock(\'fs\', () => ({\n  existsSync: vi.fn(),\n  readFileSync: vi.fn(),\n  writeFileSync: vi.fn(),\n  mkdirSync: vi.fn()\n}))\n\n// 使用\nimport * as fs from \'fs\'\n\nvi.mocked(fs.existsSync).mockReturnValue(true)\nvi.mocked(fs.readFileSync).mockReturnValue(JSON.stringify({ data: \'test\' }))\n```\n\n#### Mock Fetch\n\n```typescript\n// Mock global fetch\nglobal.fetch = vi.fn()\n\n// 使用\nvi.mocked(fetch).mockResolvedValue({\n  ok: true,\n  json: async () => ({ result: \'success\' })\n} as Response)\n```\n\n---\n\n### 7.3 WorkflowEngine 測試完整範例\n\n```typescript\n// server/src/execution/__tests__/WorkflowEngine.test.ts\n\nimport { describe, it, expect, vi, beforeEach } from \'vitest\'\nimport { WorkflowEngine } from \'../WorkflowEngine\'\nimport * as executor from \'../../executor\'\n\nvi.mock(\'../../executor\')\n\ndescribe(\'WorkflowEngine\', () => {\n  const mockEmit = vi.fn()\n  let mockExecuteNode: any\n\n  beforeEach(() => {\n    vi.clearAllMocks()\n    mockExecuteNode = vi.mocked(executor.executeNode)\n  })\n\n  describe(\'Basic Execution\', () => {\n    it(\'should execute a simple linear workflow (A → B → C)\', async () => {\n      const workflow = {\n        id: \'wf-1\',\n        nodes: [\n          { id: \'node-a\', type: \'manual-trigger\', data: { nodeType: \'manual-trigger\' } },\n          { id: \'node-b\', type: \'gemini\', data: { nodeType: \'gemini\' } },\n          { id: \'node-c\', type: \'send-telegram\', data: { nodeType: \'send-telegram\' } }\n        ],\n        edges: [\n          { id: \'e1\', source: \'node-a\', target: \'node-b\', sourceHandle: \'out-data\', targetHandle: \'in-prompt\' },\n          { id: \'e2\', source: \'node-b\', target: \'node-c\', sourceHandle: \'out-result\', targetHandle: \'in-message\' }\n        ]\n      }\n\n      // Mock executor 返回值\n      mockExecuteNode\n        .mockResolvedValueOnce({ data: \'trigger data\' })  // node-a\n        .mockResolvedValueOnce({ result: \'AI response\' })  // node-b\n        .mockResolvedValueOnce({ success: true })          // node-c\n\n      const engine = new WorkflowEngine(workflow, \'exec-1\', mockEmit)\n      await engine.run()\n\n      // 驗證執行順序\n      expect(mockExecuteNode).toHaveBeenCalledTimes(3)\n      expect(mockExecuteNode).toHaveBeenNthCalledWith(1, \'manual-trigger\', expect.any(Object), expect.any(Function))\n      expect(mockExecuteNode).toHaveBeenNthCalledWith(2, \'gemini\', expect.any(Object), expect.any(Function))\n      expect(mockExecuteNode).toHaveBeenNthCalledWith(3, \'send-telegram\', expect.any(Object), expect.any(Function))\n\n      // 驗證事件發送\n      expect(mockEmit).toHaveBeenCalledWith(\'execution:start\', expect.any(Object))\n      expect(mockEmit).toHaveBeenCalledWith(\'execution:complete\', expect.any(Object))\n    })\n\n    it(\'should handle parallel execution (A → [B, C] → D)\', async () => {\n      const workflow = {\n        id: \'wf-2\',\n        nodes: [\n          { id: \'a\', type: \'manual-trigger\', data: { nodeType: \'manual-trigger\' } },\n          { id: \'b\', type: \'gemini\', data: { nodeType: \'gemini\' } },\n          { id: \'c\', type: \'gemini\', data: { nodeType: \'gemini\' } },\n          { id: \'d\', type: \'send-telegram\', data: { nodeType: \'send-telegram\' } }\n        ],\n        edges: [\n          { id: \'e1\', source: \'a\', target: \'b\', sourceHandle: \'out-data\', targetHandle: \'in-prompt\' },\n          { id: \'e2\', source: \'a\', target: \'c\', sourceHandle: \'out-data\', targetHandle: \'in-prompt\' },\n          { id: \'e3\', source: \'b\', target: \'d\', sourceHandle: \'out-result\', targetHandle: \'in-message\' },\n          { id: \'e4\', source: \'c\', target: \'d\', sourceHandle: \'out-result\', targetHandle: \'in-message\' }\n        ]\n      }\n\n      mockExecuteNode\n        .mockResolvedValueOnce({ data: \'trigger\' })       // a\n        .mockResolvedValueOnce({ result: \'response-b\' })  // b\n        .mockResolvedValueOnce({ result: \'response-c\' })  // c\n        .mockResolvedValueOnce({ success: true })         // d\n\n      const engine = new WorkflowEngine(workflow, \'exec-2\', mockEmit)\n      await engine.run()\n\n      // 驗證 b 和 c 並行執行（都在 a 完成後執行）\n      expect(mockExecuteNode).toHaveBeenCalledTimes(4)\n\n      // 驗證 d 收到 b 和 c 的輸出\n      const dCallConfig = mockExecuteNode.mock.calls[3][1]\n      expect(dCallConfig).toMatchObject({\n        message: expect.objectContaining({\n          result: expect.any(String)\n        })\n      })\n    })\n  })\n\n  describe(\'Conditional Execution\', () => {\n    it(\'should skip node when all inputs are null\', async () => {\n      const workflow = {\n        id: \'wf-3\',\n        nodes: [\n          { id: \'a\', type: \'manual-trigger\', data: { nodeType: \'manual-trigger\' } },\n          { id: \'b\', type: \'gemini\', data: { nodeType: \'gemini\' } }\n        ],\n        edges: [\n          { id: \'e1\', source: \'a\', target: \'b\', sourceHandle: \'out-data\', targetHandle: \'in-prompt\' }\n        ]\n      }\n\n      // a 返回 null → b 應該被跳過\n      mockExecuteNode.mockResolvedValueOnce({ data: null })\n\n      const engine = new WorkflowEngine(workflow, \'exec-3\', mockEmit)\n      await engine.run()\n\n      // 驗證只執行了 a，b 被跳過\n      expect(mockExecuteNode).toHaveBeenCalledTimes(1)\n      expect(mockEmit).toHaveBeenCalledWith(\'node:skipped\', expect.objectContaining({\n        nodeId: \'b\'\n      }))\n    })\n  })\n\n  describe(\'Cascade Disable\', () => {\n    it(\'should cascade disable when all upstreams are disabled\', async () => {\n      const workflow = {\n        id: \'wf-4\',\n        nodes: [\n          { id: \'a\', type: \'manual-trigger\', data: { nodeType: \'manual-trigger\', disabled: true } },\n          { id: \'b\', type: \'gemini\', data: { nodeType: \'gemini\' } }  // 非資料類節點\n        ],\n        edges: [\n          { id: \'e1\', source: \'a\', target: \'b\', sourceHandle: \'out-data\', targetHandle: \'in-prompt\' }\n        ]\n      }\n\n      const engine = new WorkflowEngine(workflow, \'exec-4\', mockEmit)\n      await engine.run()\n\n      // 驗證 a 和 b 都被跳過\n      expect(mockExecuteNode).not.toHaveBeenCalled()\n      expect(mockEmit).toHaveBeenCalledWith(\'node:skipped\', expect.objectContaining({\n        nodeId: \'a\'\n      }))\n      expect(mockEmit).toHaveBeenCalledWith(\'node:skipped\', expect.objectContaining({\n        nodeId: \'b\'\n      }))\n      expect(mockEmit).toHaveBeenCalledWith(\'node:log\', expect.objectContaining({\n        nodeId: \'b\',\n        message: expect.stringContaining(\'級聯停用\')\n      }))\n    })\n  })\n\n  describe(\'Node Cache\', () => {\n    it(\'should use cached result when useCachedResult is true\', async () => {\n      const workflow = {\n        id: \'wf-5\',\n        nodes: [\n          { id: \'a\', type: \'gemini\', data: { nodeType: \'gemini\', useCachedResult: true } }\n        ],\n        edges: []\n      }\n\n      const nodeCache = {\n        \'a\': { result: \'cached data from previous run\' }\n      }\n\n      const engine = new WorkflowEngine(workflow, \'exec-5\', mockEmit, nodeCache)\n      await engine.run()\n\n      // 驗證沒有實際執行 executor\n      expect(mockExecuteNode).not.toHaveBeenCalled()\n\n      // 驗證發送了快取事件\n      expect(mockEmit).toHaveBeenCalledWith(\'node:log\', expect.objectContaining({\n        nodeId: \'a\',\n        message: expect.stringContaining(\'使用快取結果\')\n      }))\n\n      expect(mockEmit).toHaveBeenCalledWith(\'node:done\', expect.objectContaining({\n        nodeId: \'a\',\n        result: { result: \'cached data from previous run\' },\n        cached: true,\n        duration: 0\n      }))\n    })\n  })\n\n  describe(\'Error Handling\', () => {\n    it(\'should mark execution as failed when node throws error\', async () => {\n      const workflow = {\n        id: \'wf-6\',\n        nodes: [\n          { id: \'a\', type: \'gemini\', data: { nodeType: \'gemini\' } }\n        ],\n        edges: []\n      }\n\n      mockExecuteNode.mockRejectedValue(new Error(\'API error\'))\n\n      const engine = new WorkflowEngine(workflow, \'exec-6\', mockEmit)\n\n      await expect(engine.run()).rejects.toThrow(\'API error\')\n\n      expect(mockEmit).toHaveBeenCalledWith(\'node:error\', expect.objectContaining({\n        nodeId: \'a\',\n        error: \'API error\'\n      }))\n\n      expect(mockEmit).toHaveBeenCalledWith(\'execution:failed\', expect.objectContaining({\n        error: \'API error\'\n      }))\n    })\n  })\n\n  describe(\'Trigger Modes\', () => {\n    it(\'should run triggers in fallback mode (stop at first success)\', async () => {\n      const workflow = {\n        id: \'wf-7\',\n        triggerMode: \'fallback\',\n        nodes: [\n          { id: \'t1\', type: \'youtube-monitor\', data: { nodeType: \'youtube-monitor\', category: \'trigger\' } },\n          { id: \'t2\', type: \'youtube-recent-videos\', data: { nodeType: \'youtube-recent-videos\', category: \'trigger\' } },\n          { id: \'b\', type: \'gemini\', data: { nodeType: \'gemini\' } }\n        ],\n        edges: [\n          { id: \'e1\', source: \'t1\', target: \'b\', sourceHandle: \'out-video\', targetHandle: \'in-prompt\' },\n          { id: \'e2\', source: \'t2\', target: \'b\', sourceHandle: \'out-video\', targetHandle: \'in-prompt\' }\n        ]\n      }\n\n      // t1 失敗，t2 成功\n      mockExecuteNode\n        .mockRejectedValueOnce(new Error(\'Monitor failed\'))\n        .mockResolvedValueOnce({ video: \'success video\' })  // t2\n        .mockResolvedValueOnce({ result: \'AI response\' })   // b\n\n      const engine = new WorkflowEngine(workflow, \'exec-7\', mockEmit)\n      await engine.run()\n\n      // 驗證只執行了 2 個觸發器（t1 失敗，t2 成功後停止）\n      expect(mockExecuteNode).toHaveBeenCalledTimes(3)  // t1, t2, b\n      expect(mockEmit).toHaveBeenCalledWith(\'node:log\', expect.objectContaining({\n        message: expect.stringContaining(\'觸發器 2 成功\')\n      }))\n    })\n\n    it(\'should run triggers in sequential mode (run all)\', async () => {\n      const workflow = {\n        id: \'wf-8\',\n        triggerMode: \'sequential\',\n        nodes: [\n          { id: \'t1\', type: \'youtube-monitor\', data: { nodeType: \'youtube-monitor\', category: \'trigger\' } },\n          { id: \'t2\', type: \'youtube-recent-videos\', data: { nodeType: \'youtube-recent-videos\', category: \'trigger\' } }\n        ],\n        edges: []\n      }\n\n      mockExecuteNode\n        .mockResolvedValueOnce({ video: \'video1\' })\n        .mockResolvedValueOnce({ video: \'video2\' })\n\n      const engine = new WorkflowEngine(workflow, \'exec-8\', mockEmit)\n      await engine.run()\n\n      // 驗證執行了所有觸發器\n      expect(mockExecuteNode).toHaveBeenCalledTimes(2)\n    })\n  })\n\n  describe(\'Edge Cases\', () => {\n    it(\'should throw error when no start nodes found\', async () => {\n      const workflow = {\n        id: \'wf-9\',\n        nodes: [\n          { id: \'a\', type: \'gemini\', data: { nodeType: \'gemini\' } },\n          { id: \'b\', type: \'send-telegram\', data: { nodeType: \'send-telegram\' } }\n        ],\n        edges: [\n          { id: \'e1\', source: \'a\', target: \'b\', sourceHandle: \'out-result\', targetHandle: \'in-message\' }\n        ]\n      }\n\n      // a 沒有輸出邊 → 沒有起始節點\n      workflow.edges = [\n        { id: \'e1\', source: \'a\', target: \'b\', sourceHandle: \'out-result\', targetHandle: \'in-message\' }\n      ]\n\n      const engine = new WorkflowEngine(workflow, \'exec-9\', mockEmit)\n\n      await expect(engine.run()).rejects.toThrow(\'沒有起始節點\')\n    })\n\n    it(\'should ignore isolated nodes (no connections)\', async () => {\n      const workflow = {\n        id: \'wf-10\',\n        nodes: [\n          { id: \'a\', type: \'manual-trigger\', data: { nodeType: \'manual-trigger\' } },\n          { id: \'isolated\', type: \'gemini\', data: { nodeType: \'gemini\' } }  // 孤立節點\n        ],\n        edges: []\n      }\n\n      mockExecuteNode.mockResolvedValue({ data: \'test\' })\n\n      const engine = new WorkflowEngine(workflow, \'exec-10\', mockEmit)\n      await engine.run()\n\n      // 孤立節點不會被執行\n      expect(mockExecuteNode).toHaveBeenCalledTimes(1)  // 只執行 a\n    })\n  })\n})\n```\n\n---\n\n### 7.4 Executor 測試模板\n\n```typescript\n// server/src/executors/__tests__/executor-template.test.ts\n\nimport { describe, it, expect, vi, beforeEach } from \'vitest\'\nimport { executeYourExecutor } from \'../your-executor\'\n\n// Mock 外部依賴\nvi.mock(\'dependency-module\')\n\ndescribe(\'Your Executor\', () => {\n  const mockEmit = vi.fn()\n\n  beforeEach(() => {\n    vi.clearAllMocks()\n  })\n\n  describe(\'Happy Path\', () => {\n    it(\'should execute successfully with valid config\', async () => {\n      // Arrange\n      const config = {\n        param1: \'value1\',\n        param2: \'value2\'\n      }\n\n      // Act\n      const result = await executeYourExecutor(config, mockEmit)\n\n      // Assert\n      expect(result).toMatchObject({\n        expectedKey: expect.any(String)\n      })\n\n      expect(mockEmit).toHaveBeenCalledWith(\'node:log\', \n        expect.objectContaining({ message: expect.any(String) })\n      )\n    })\n  })\n\n  describe(\'Error Handling\', () => {\n    it(\'should throw error when required param is missing\', async () => {\n      const config = {}\n\n      await expect(\n        executeYourExecutor(config, mockEmit)\n      ).rejects.toThrow(\'param is required\')\n    })\n\n    it(\'should handle external API failure gracefully\', async () => {\n      // Mock API 失敗\n      // ...\n    })\n  })\n\n  describe(\'Edge Cases\', () => {\n    it(\'should handle empty input\', async () => {\n      // 測試空輸入\n    })\n\n    it(\'should handle large input\', async () => {\n      // 測試大量資料\n    })\n  })\n})\n```\n\n---\n\n### 7.5 前端 Store 測試模板\n\n```typescript\n// src/stores/__tests__/store-template.test.ts\n\nimport { describe, it, expect, beforeEach } from \'vitest\'\nimport { setActivePinia, createPinia } from \'pinia\'\nimport { useYourStore } from \'../your-store\'\n\ndescribe(\'Your Store\', () => {\n  beforeEach(() => {\n    setActivePinia(createPinia())\n    localStorage.clear()\n  })\n\n  it(\'should initialize with default state\', () => {\n    const store = useYourStore()\n\n    expect(store.items).toEqual([])\n    expect(store.currentItem).toBeNull()\n  })\n\n  it(\'should add item correctly\', () => {\n    const store = useYourStore()\n\n    const item = store.addItem(\'Test Item\')\n\n    expect(item.name).toBe(\'Test Item\')\n    expect(store.items).toHaveLength(1)\n  })\n\n  it(\'should persist to localStorage\', () => {\n    const store = useYourStore()\n\n    store.addItem(\'Persistent Item\')\n\n    const stored = localStorage.getItem(\'your_store_key\')\n    expect(stored).toBeTruthy()\n    expect(JSON.parse(stored!)).toHaveLength(1)\n  })\n})\n```\n\n---\n\n## 8. 總結與 Quick Wins\n\n### 🎯 Quick Wins（5 天內可完成，高價值）\n\n#### 1. WorkflowEngine 基礎測試（1 天）\n**價值**：涵蓋整個系統的核心邏輯\n```bash\n# 建立測試檔案\nserver/src/execution/__tests__/WorkflowEngine.test.ts\n\n# 測試場景（優先級排序）\n1. ✅ 基礎執行流程（A → B → C）\n2. ✅ 依賴解析（並行執行）\n3. ✅ 條件執行（null 判斷）\n4. ✅ 錯誤處理\n```\n\n**預期收益**：\n- 發現 30%+ 的潛在 bug\n- 為重構提供安全網\n- 提升開發信心\n\n---\n\n#### 2. Gemini Executor 測試（0.5 天）\n**價值**：最常用的 AI 節點\n```bash\nserver/src/executors/__tests__/gemini.test.ts\n\n# 關鍵測試\n1. ✅ API Key 模式\n2. ✅ 429 重試機制\n3. ✅ 錯誤處理\n```\n\n**預期收益**：\n- 防止 API Key 配置錯誤\n- 確保重試邏輯可靠\n- 減少生產環境錯誤\n\n---\n\n#### 3. Send Telegram Executor 測試（0.5 天）\n**價值**：通知出口，失敗影響大\n```bash\nserver/src/executors/__tests__/send-telegram.test.ts\n\n# 關鍵測試\n1. ✅ 文字/圖片發送\n2. ✅ Markdown 解析失敗重試\n3. ✅ Inline Keyboard\n```\n\n**預期收益**：\n- 確保通知可靠送達\n- 避免 Markdown 解析錯誤\n- 提升使用者體驗\n\n---\n\n#### 4. API Endpoints 整合測試（1 天）\n**價值**：保證前後端協作正確\n```bash\nserver/src/__tests__/api.integration.test.ts\n\n# 關鍵 Endpoints\n1. ✅ POST /api/workflow/run\n2. ✅ POST /api/execute\n3. ✅ GET /api/health\n```\n\n**預期收益**：\n- 確保 API 契約穩定\n- 減少前後端整合問題\n- 支援 API 文件生成\n\n---\n\n#### 5. 設置 CI/CD（0.5 天）\n**價值**：自動化測試，持續保障品質\n```yaml\n# .github/workflows/test.yml\n\n# 關鍵步驟\n1. ✅ 安裝依賴\n2. ✅ 執行測試\n3. ✅ 生成覆蓋率報告\n4. ✅ PR 時自動檢查\n```\n\n**預期收益**：\n- 自動發現 PR 中的問題\n- 測試覆蓋率趨勢可視化\n- 提升協作效率\n\n---\n\n### 📊 測試投資回報率（ROI）\n\n| 測試項目 | 投入時間 | 預防的問題數 | ROI |\n|---------|---------|------------|-----|\n| WorkflowEngine | 1 天 | 10+ bugs | ⭐⭐⭐⭐⭐ |\n| Gemini Executor | 0.5 天 | 5+ bugs | ⭐⭐⭐⭐⭐ |\n| Send Telegram | 0.5 天 | 3+ bugs | ⭐⭐⭐⭐ |\n| API Integration | 1 天 | 5+ bugs | ⭐⭐⭐⭐ |\n| CI/CD 設置 | 0.5 天 | 持續 | ⭐⭐⭐⭐⭐ |\n\n**總計**：3.5 天投入，預防 23+ bugs，長期 ROI 極高\n\n---\n\n### 🚀 立即行動指南\n\n**Day 1 - 建立測試基礎**\n```bash\n# 1. 安裝依賴\nnpm install -D vitest @vitest/ui c8\ncd server && npm install -D vitest\n\n# 2. 建立 vitest.config.ts\n# 3. 更新 package.json scripts\n# 4. 測試框架是否可運行\nnpm test\n```\n\n**Day 2 - WorkflowEngine 測試**\n```bash\n# 建立測試檔案\nmkdir -p server/src/execution/__tests__\ntouch server/src/execution/__tests__/WorkflowEngine.test.ts\n\n# 複製上面的測試範例程式碼\n# 執行測試\nnpm test WorkflowEngine\n```\n\n**Day 3 - Executors 測試**\n```bash\n# 建立測試檔案\nmkdir -p server/src/executors/__tests__\ntouch server/src/executors/__tests__/gemini.test.ts\ntouch server/src/executors/__tests__/send-telegram.test.ts\n\n# 執行測試\nnpm test gemini\n```\n\n**Day 4 - API 整合測試**\n```bash\n# 安裝 supertest\ncd server && npm install -D supertest @types/supertest\n\n# 建立測試檔案\ntouch server/src/__tests__/api.integration.test.ts\n\n# 執行測試\nnpm test api.integration\n```\n\n**Day 5 - CI/CD 設置**\n```bash\n# 建立 GitHub Actions\nmkdir -p .github/workflows\ntouch .github/workflows/test.yml\n\n# 提交並推送\ngit add .\ngit commit -m "chore: add test framework and CI/CD"\ngit push\n\n# 檢查 GitHub Actions 是否運行\n```\n\n---\n\n### 📈 測試成熟度路線圖\n\n**Level 1: 基礎測試（Week 1-2）**\n- ✅ 測試框架設置\n- ✅ WorkflowEngine 核心測試\n- ✅ 3 個關鍵 Executor 測試\n- ✅ CI/CD 基礎設置\n- **目標覆蓋率**：40%\n\n**Level 2: 完整覆蓋（Week 3-4）**\n- ✅ 所有 Executors 測試\n- ✅ 前端 Store 測試\n- ✅ 組件測試\n- **目標覆蓋率**：70%\n\n**Level 3: 高品質測試（Week 5+）**\n- ✅ E2E 測試\n- ✅ 效能測試\n- ✅ 視覺回歸測試\n- **目標覆蓋率**：85%+\n\n---\n\n### 💡 關鍵成功因素\n\n1. **管理層支持**：測試需要時間投入，需要得到優先級支持\n2. **團隊共識**：所有開發者都遵守「新功能必須有測試」\n3. **持續投入**：測試是持續工程，不是一次性任務\n4. **工具支援**：良好的測試工具和 CI/CD 流程\n5. **文化建立**：將測試視為交付品質的一部分\n\n---\n\n### 📝 附錄：測試檢查清單\n\n**新功能開發前**：\n- [ ] 理解現有測試結構\n- [ ] 確認測試框架可運行\n- [ ] 規劃測試範圍\n\n**開發過程中**：\n- [ ] 先寫測試（TDD）或同步寫測試\n- [ ] 執行測試確保通過\n- [ ] 檢查測試覆蓋率\n\n**提交 PR 前**：\n- [ ] 所有測試通過\n- [ ] 新增代碼測試覆蓋率 > 70%\n- [ ] CI 檢查通過\n- [ ] 更新測試文件（如有必要）\n\n**Code Review 時**：\n- [ ] 檢查測試品質\n- [ ] 確認測試覆蓋關鍵路徑\n- [ ] 驗證邊界條件測試\n- [ ] 檢查 Mock 使用是否合理\n\n---\n\n## 結論\n\nFlowCraft 專案目前處於 **零測試狀態**，這對於一個工作流自動化引擎來說是高風險的。但好消息是，專案的程式碼結構清晰、模組化良好，非常適合添加測試。\n\n**建議採取的行動**：\n1. **立即開始**：從 Quick Wins 開始（5 天內完成基礎測試）\n2. **持續投入**：每週投入 20% 時間在測試上\n3. **建立文化**：新功能必須附帶測試\n4. **監控進度**：追蹤測試覆蓋率趨勢\n\n透過系統化的測試策略，FlowCraft 可以在 3-4 週內達到 **70% 測試覆蓋率**，大幅提升專案的穩定性和可維護性。\n\n---\n\n目前專案：flowcraft | /Users/yaja/projects/flowcraft'}]
